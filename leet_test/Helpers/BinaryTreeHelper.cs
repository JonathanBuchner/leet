using leet.LeetCode.Models;
using System;
using System.Collections.Generic;
using System.Text;

namespace leet_test.Helpers
{
    /*public class BinaryTreeHelper
    {
        private int?[] arr;
        public BinaryTreeHelper(int?[] arr)
        {
            this.arr = arr;
            
        }
        public TreeNode MakeTree(int index = 0)
        {
            if (arr.Length == 0 || arr[0] == null)
            {
                return null;
            }
            else
            {
                var node = new TreeNode((int)arr[index]);
            }

            index++;
            if (index <= arr.Length)
            {

            }

        }

        private TreeNode MakeNode(int index)
        {
            if (index >= arr.Length)
            {
                return null;
            }
            else if (arr[index] == null)
            {
                return null;
            }
            else
            {
                return new TreeNode((int)arr[index]);
            }
        }
    }*/
}
